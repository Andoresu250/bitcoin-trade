<p id="notice"><%= notice %></p>

<h1>Sales</h1>

<table>
  <thead>
    <tr>
      <th>Person</th>
      <th>Btc</th>
      <th>Value</th>
      <th>Country</th>
      <th>State</th>
      <th>Wallet url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sales.each do |sale| %>
      <tr>
        <td><%= sale.person %></td>
        <td><%= sale.btc %></td>
        <td><%= sale.value %></td>
        <td><%= sale.country %></td>
        <td><%= sale.state %></td>
        <td><%= sale.wallet_url %></td>
        <td><%= link_to 'Show', sale %></td>
        <td><%= link_to 'Edit', edit_sale_path(sale) %></td>
        <td><%= link_to 'Destroy', sale, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Sale', new_sale_path %>
